spring.application.name=audiotracker-api

# ========= DATABASE CONFIG =========
spring.datasource.url=jdbc:mysql://localhost:3306/audiobook_tracker?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=fiqkir^.

# ========= JPA / HIBERNATE =========
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ========= SERVER PORT (optional) =========
server.port=8081

# ========= LOGGING (optional, for cleaner console) =========
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE
